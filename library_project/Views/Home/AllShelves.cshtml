@model IEnumerable<library_pr.shelf>

@{
    ViewData["Title"] = "AllShelves";
}

<h1>AllShelves</h1>

<p>
    @* <a asp-action="shelf">Create New</a> *@
    @Html.ActionLink("Create New", "shelf", new {  id=ViewBag.id })
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hight)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hight)
            </td>
            <td>
                    | @Html.ActionLink("רשימת ספרים", "AllBooks", new { /* id=item.PrimaryKey */ }) |
               @*  @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
            </td>
        </tr>
}
    </tbody>
</table>
